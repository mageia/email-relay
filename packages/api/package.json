{
  "name": "@email-relay/api",
  "type": "module",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    },
    "./*": {
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "test": "vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "@email-relay/auth": "workspace:*",
    "@email-relay/db": "workspace:*",
    "@email-relay/env": "workspace:*",
    "@email-relay/mail": "workspace:*",
    "@orpc/client": "catalog:",
    "@orpc/openapi": "catalog:",
    "@orpc/server": "catalog:",
    "@orpc/zod": "catalog:",
    "dotenv": "catalog:",
    "zod": "catalog:",
    "drizzle-orm": "^0.45.1"
  },
  "devDependencies": {
    "@email-relay/config": "workspace:*",
    "hono": "catalog:",
    "typescript": "^5"
  }
}
